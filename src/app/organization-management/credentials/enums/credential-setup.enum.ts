export enum CredentialSetupSystemEnum {
  All = 1,
  IRP,
  VMS,
}

export enum DropdownsList {
  Regions = 'regions',
  Locations = 'locations',
  Departments = 'departments',
  Groups = 'groups',
  Types = 'types',
}
