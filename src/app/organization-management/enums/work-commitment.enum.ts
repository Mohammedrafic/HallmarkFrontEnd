export enum WORK_COMMITMENT_ACTIONS {
  GET_COMMITMENTS_BY_PAGE = '[work commitment] Get commitments by page',
  SAVE_COMMITMENT = '[work commitment] Save Commitment',
  DELETE_COMMITMENT = '[work commitment] Delete Commitment',
  UPDATE_PAGE_AFTER_SUCCESS_ACTION = '[work commitment] Update page after success action',
}
