export enum IrpOrderType {
  LongTermAssignment = 3,
  PerDiem = 1
}
//todo: change to PerDiem = 10, when be will be ready

export enum ButtonType {
  Edit = 0,
  RemoveContact = 1,
  RemoveWorkLocation = 2,
  addContact = 3,
  addMore = 4,
}
