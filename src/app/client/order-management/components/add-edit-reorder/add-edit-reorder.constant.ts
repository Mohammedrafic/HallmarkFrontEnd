export const MandatoryCandidateErrorMessage = (camdidateName: string) => {
  return `Candidate ${camdidateName} can't be removed from re-order`;
};

export const MandatoryAgencyErrorMessage = (agencyName: string) => {
  return `Agency ${agencyName} can't be removed from re-order`;
};
