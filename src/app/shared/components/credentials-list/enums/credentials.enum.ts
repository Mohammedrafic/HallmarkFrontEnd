export enum CredentialsDialogTitle {
  Add = 0,
  Edit = 1
}

export enum CredentialListAction {
  GetCredentialDataSources = '[credentialList] Get Credentials Data Sources',
  ExportCredentialList = '[credentialList] Export Credential list',
}
